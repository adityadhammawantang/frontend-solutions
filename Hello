function hello (name) {
  if (!name) { // !name means if there is no name, carry out this statement    // only change the text in the double quotes
    return "Hello, World!";
  } else {
// only change the text in the double quotes
    return "Hello, "(name)"!";
  }
}
